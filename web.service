[Unit]
Description=x.st
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStartPre=-docker rm -f web
# TODO ExecStartPre=docker pull hrldcpr/x.st-web
ExecStart=docker run --name web -p 80:80 web

[Install]
WantedBy=multi-user.target
